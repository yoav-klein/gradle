/*
 * This file was generated by the Gradle 'init' task.
 *
 * This is a general purpose Gradle build.
 * To learn more about Gradle by exploring our Samples at https://docs.gradle.org/8.7/samples
 */


/* registering a task of no specific type */
tasks.register('hello') {
    doLast {
        println "HELLO"
    }
}

/* registering a task of type Copy. configuring the task as part of the register method */
tasks.register('copy1', Copy) {
    from "resources"
    into "target"
    include "**/*.txt", "**/*.xml", "**/*.properties"

}

/* registering a task of type Copy. configuring the task using the named method */

tasks.register('copy2', Copy) 

tasks.named('copy2') {
    from "resources"
    into "target"
    include "**/*.txt", "**/*.xml", "**/*.properties"
}

/* creating a task type */
abstract class HelloTask extends DefaultTask {             
    @TaskAction
    void sayHello() {
        println "HEellllo"
    }
}

tasks.register('myhello', HelloTask)